<%= form_for(cover, local: true, html: { multipart: true }) do |form| %>
  <% if cover.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(cover.errors.count, "error") %> prohibited this cover from being saved:</h2>

      <ul>
      <% cover.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :titulo %>
    <%= form.text_field :title, id: :cover_title %>
  </div>

  <div class="field">
    <%= form.label :descripcion %>
    <%= form.text_field :description, id: :cover_description %>
  </div>

  <div class="field">
    <%= form.file_field :image %>
  </div>

  <div class="actions">
    <%= form.submit :enviar %>
  </div>
<% end %>
